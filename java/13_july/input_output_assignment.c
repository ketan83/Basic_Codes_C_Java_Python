#include<stdio.h>
void main() {

/*****************************Answer 1************************** C pgm that takes radius of circle as input from user, and calculates it's area and circumference 

	int r;
	float  a,c;
	printf("Enter the radius : \n");
	scanf("%d",&r);

	a  = 3.14 * r*r;
	printf("Area of Circle is %f\n",a);

	c = 2 * 3.14 * r;
	printf("Circumference of Circle is %f\n",c);

Output :
ketan@ketan-tuf-fx505dt:~/ppa5$ vim input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ cc input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ ./a.out
Enter the radius :
4
Area of Circle is 50.240002
Circumference of Circle is 25.120001

*/
	/******************Answer 3************************* c program that accepts a character from the user and print whether that character is a consonent or a vowel.  
	char ch;
	printf("ENter a character : ");
	scanf("%c",&ch);
	printf("%c is a vowel \n",ch);

	ketan@ketan-tuf-fx505dt:~/ppa5$ vim input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ cc input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ ./a.out
ENter a character : a
a is a vowel
 */
	/*****************Answer 4******************* C program that  accepts input of every data type (one of each) from user and prints them 
	 int x;
        printf("ENter an integer value : ");
        scanf("%d",&x);
        printf("%d is an integer value \n",x);

Output :
ketan@ketan-tuf-fx505dt:~/ppa5$ vim input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ cc input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ ./a.out
ENter an integer value : 10
10 is an integer value

*/

	/*****************Answer 5************************* C program that accepts 2 no.s from the user and prints the average of those 2 no.s
	         int x,y,z;
        printf("ENter  integer value in y & z ");
        scanf("%d%d",&y,&z);
	x = y + z;
	x/=2;
        printf("Average of %d & %d is %d \n",y,z,x);


	Output :ketan@ketan-tuf-fx505dt:~/ppa5$ vim input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ cc input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ ./a.out
ENter  integer value in y & z 10 20
Average of 10 & 20 is 15 
*/

	/*********************Answer 2********************************** C program thats accepts the time in hours and minutes from user and prints as total number of Minutes
     int x , y,z;
     printf("ENter no. of hours : ");
     scanf("%d",&x);
     x*=60;
     printf("ENter no. of minutes : ");
     scanf("%d",&y);
     z = x +y;
     printf("Total no. of minutes : %d\n",z);

Output : 
     ketan@ketan-tuf-fx505dt:~/ppa5$ vim input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ cc input_output_assignment.c
ketan@ketan-tuf-fx505dt:~/ppa5$ ./a.out
ENter no. of hours : 2
ENter no. of minutes : 30
Total no. of minutes : 150
ketan@ketan-tuf-fx505dt:~/ppa5$

*/
    
}
